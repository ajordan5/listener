#!/usr/bin/env python3
import asyncio
from boat import Boat
# from follow_me_example import FollowMe

if __name__ == "__main__":
    boat = Boat()
    # followMe = FollowMe()
    # loop = asyncio.get_event_loop()
    # loop.run_until_complete(followMe.fly_drone())